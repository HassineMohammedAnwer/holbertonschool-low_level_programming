#include <stdio.h>
#include "main.h"

/**
 * mul - function that multiplies two integers
 * @a: The integer to test with
 * @b: The integer to test with
 * Return: the multiplication result x
 */

int mul(int a, int b)
{
int x;
x = a * b;
return (x);
}
