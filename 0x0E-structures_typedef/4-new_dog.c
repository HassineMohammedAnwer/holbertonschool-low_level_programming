#include "dog.h"
#include <stdio.h>
/**
 * *new_dog -  a function that creates a new dog.
 * @name: string
 * @age: float
 * @owner: string
 * Return: new dog
 * Description: betty
 */
dog_t *new_dog(char *name, float age, char *owner)
{
	if (d != NULL)
	{
		if (d->name == NULL)
			printf("Name: (nil)\n");
		else
			printf("Name: %s\n", d->name);
		 printf("Age: %f\n", d->age);
		if (d->owner == NULL)
			printf("Owner: (nil)\n");
		else
			printf("Owner: %s\n", d->owner);
	}
}
