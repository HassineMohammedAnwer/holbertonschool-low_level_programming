#include <stdio.h>
#include "main.h"

/**
* times_table - prints the last digit of the random
*/
void times_table(void)
{
int x, y;
for (x = 0; x <= 9; x++)
{
for (y = 0; y <= 9; y++)
{
_putchar (y);
}
_putchar (x);
}
}
