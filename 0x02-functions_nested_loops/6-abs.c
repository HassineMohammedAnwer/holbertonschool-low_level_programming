#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
* _abs - prints the last digit of the random
* @n : integer
* Return: Always 0 (Success)
* the string and is greater than 5
* the string and is 0
* the string and is less than 6 and not 0
*/
int _abs(int n)
{
if (n < 0)
{
n = -n;
return (n);
}
else
return (n);
}
