#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the opcodes of its own main fuction
 * @argc: size of argv
 * @argv: array of arguments
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	if (argc != 2)
	{
		printf("Error\n");
		exit(1);
	}

	return (0);
}
